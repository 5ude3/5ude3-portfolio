/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/5ude3-portfolio",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };

module.exports = nextConfig;
